apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio
  namespace: storage
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`minio.local`)
      kind: Rule
      services:
        - name: minio
          port: 9000
